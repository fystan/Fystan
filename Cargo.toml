[package]
name = "fystan"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.48", features = ["derive"] }
lazy_static = "1.5.0"
rand = "0.8.5"

[profile.release]
opt-level = 3

[dev-dependencies]
